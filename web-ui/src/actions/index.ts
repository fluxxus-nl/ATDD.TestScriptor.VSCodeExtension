import updateBackendMode from './updateBackendMode';
import updateCurrEntry from './updateCurrEntry';
import updateEntries from './updateEntries';
import addEntry from './addEntry';

export const Actions = {
    addEntry: addEntry,
    updateEntries: updateEntries,
    updateCurrEntry: updateCurrEntry,
    updateBackendMode: updateBackendMode
}
